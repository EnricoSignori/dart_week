main() {
//  testWidgets('BalancePanelWidget has message', (WidgetTester tester) async {
//    await tester.pumpWidget(buildTestableWidget(BalancePanelWidget()));
//    final textFinder = find.text('BalancePanel');
//    expect(textFinder, findsOneWidget);
//  });
}
