import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:dart_week_mobile/app/modules/transaction/transaction_module.dart';

void main() {
  initModule(TransactionModule());
  // BalancePanelController balancepanel;
  //
  setUp(() {
    //     balancepanel = TransactionModule.to.get<BalancePanelController>();
  });

  group('BalancePanelController Test', () {
    //   test("First Test", () {
    //     expect(balancepanel, isInstanceOf<BalancePanelController>());
    //   });

    //   test("Set Value", () {
    //     expect(balancepanel.value, equals(0));
    //     balancepanel.increment();
    //     expect(balancepanel.value, equals(1));
    //   });
  });
}
