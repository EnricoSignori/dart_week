import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:dart_week_mobile/app/modules/transaction/transaction_module.dart';

void main() {
  initModule(TransactionModule());
  // TransactionController transaction;
  //
  setUp(() {
    //     transaction = TransactionModule.to.get<TransactionController>();
  });

  group('TransactionController Test', () {
    //   test("First Test", () {
    //     expect(transaction, isInstanceOf<TransactionController>());
    //   });

    //   test("Set Value", () {
    //     expect(transaction.value, equals(0));
    //     transaction.increment();
    //     expect(transaction.value, equals(1));
    //   });
  });
}
