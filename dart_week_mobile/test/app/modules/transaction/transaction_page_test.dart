import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('TransactionPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TransactionPage(title: 'Transaction')));
    //  final titleFinder = find.text('Transaction');
    //  expect(titleFinder, findsOneWidget);
  });
}
